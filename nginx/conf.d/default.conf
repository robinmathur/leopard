# Default configuration placeholder
# Main configuration is in /etc/nginx/nginx.conf

# This file is included by nginx.conf but can be used for additional server blocks
# or location blocks if needed

# Docker Compose Environment Variables Template
# Copy this file to .env and fill in with actual values
#
# Usage:
#   cp .env.docker.example .env
#   nano .env  # Edit with your actual values
#
# IMPORTANT: Never commit .env files to version control

# ============================================================================
# Application Environment
# ============================================================================
DJANGO_ENV=production  # Options: development, staging, production
COMPOSE_PROJECT_NAME=leopard

# ============================================================================
# Database Configuration (PostgreSQL)
# ============================================================================
DB_NAME=leopard_production
DB_USER=leopard_user
DB_PASSWORD=CHANGE_THIS_TO_A_STRONG_PASSWORD  # Generate a strong password!
DB_HOST=postgres  # Docker service name (don't change for Docker)
DB_PORT=5432

# ============================================================================
# Django Configuration
# ============================================================================
# Generate a strong secret key with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=GENERATE_AND_PASTE_A_STRONG_SECRET_KEY_HERE

# Debug mode (NEVER set to True in production!)
DEBUG=False

# ============================================================================
# Domain Configuration (Multi-tenant Architecture)
# ============================================================================
# Pattern: tenant.{APP_SUBDOMAIN}.{BASE_DOMAIN}
# Example: acme.immigrate.company.com
APP_SUBDOMAIN=immigrate
BASE_DOMAIN=company.com  # Change to your actual domain

# ============================================================================
# Frontend Environment Variables
# ============================================================================
VITE_APP_SUBDOMAIN=immigrate
VITE_BASE_DOMAIN=company.com  # Change to your actual domain
VITE_ENVIRONMENT=production
# VITE_API_BASE_URL=  # Optional: Manual API URL override (leave empty for auto-detection)

# ============================================================================
# Application Feature Toggles
# ============================================================================
TASKS_DUE_SOON_DEFAULT_DAYS=3
TASKS_INCLUDE_OVERDUE_DEFAULT=true
NOTIFICATIONS_INCLUDE_READ_DEFAULT=true
NOTIFICATION_STREAM_ALLOWED_ORIGIN=https://*.company.com  # Change to your domain

# ============================================================================
# SSL/TLS Configuration (for Let's Encrypt)
# ============================================================================
CERTBOT_EMAIL=admin@company.com  # Email for Let's Encrypt notifications
CERTBOT_DOMAIN=immigrate.company.com  # Main domain for SSL certificate

# ============================================================================
# Optional: Superuser Auto-creation (Development Only)
# ============================================================================
# CREATE_SUPERUSER=false  # Set to true to auto-create admin user on first run
# SUPERUSER_USERNAME=admin
# SUPERUSER_EMAIL=admin@company.com
# SUPERUSER_PASSWORD=admin  # NEVER use this in production!

# ============================================================================
# Optional: Logging Configuration
# ============================================================================
# LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

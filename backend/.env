# Local Development Environment
SECRET_KEY=dev-secret-key-not-for-production
DEBUG=True

# Database (PostgreSQL)
DB_NAME=leopard
DB_USER=leopard
DB_PASSWORD=leopard
DB_HOST=localhost
DB_PORT=5432

# 4-Level Subdomain Configuration
# Domain structure: tenant.app.company.com
APP_SUBDOMAIN=immigrate
BASE_DOMAIN=localhost

# For production, change to:
# BASE_DOMAIN=company.com
# This will create: tenant.app.company.com
